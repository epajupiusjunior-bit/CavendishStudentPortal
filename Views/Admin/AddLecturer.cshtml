@{
    ViewData["Title"] = "Add New Lecturer";
}
<div class="max-w-2xl mx-auto">
    <h2 class="text-3xl font-semibold mb-8">Add New Lecturer</h2>
    
    <div class="glass rounded-3xl p-10">
        <form asp-action="AddLecturer" method="post">
            <div class="grid grid-cols-2 gap-8">
                <div>
                    <label class="block text-sm font-medium mb-2">Full Name</label>
                    <input name="FullName" class="w-full px-5 py-4 rounded-2xl border focus:border-[#4E93CC]" required />
                </div>
                <div>
                    <label class="block text-sm font-medium mb-2">Teacher ID</label>
                    <input name="Username" class="w-full px-5 py-4 rounded-2xl border focus:border-[#4E93CC]" required />
                </div>
                <div>
                    <label class="block text-sm font-medium mb-2">Password</label>
                    <input name="Password" type="password" class="w-full px-5 py-4 rounded-2xl border focus:border-[#4E93CC]" required />
                </div>
                <div>
                    <label class="block text-sm font-medium mb-2">Department</label>
                    <input name="Department" class="w-full px-5 py-4 rounded-2xl border focus:border-[#4E93CC]" placeholder="e.g. Fine Arts" required />
                </div>
                <div>
                    <label class="block text-sm font-medium mb-2">Email</label>
                    <input name="Email" type="email" class="w-full px-5 py-4 rounded-2xl border focus:border-[#4E93CC]" />
                </div>
                <div>
                    <label class="block text-sm font-medium mb-2">Phone</label>
                    <input name="Phone" class="w-full px-5 py-4 rounded-2xl border focus:border-[#4E93CC]" />
                </div>
            </div>

            <button type="submit" class="mt-12 w-full bg-green-600 text-white py-5 rounded-3xl font-semibold hover:bg-green-700 transition">
                Add Lecturer
            </button>
        </form>
    </div>
</div>